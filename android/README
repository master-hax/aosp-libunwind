This version of libunwind based on libunwind 1.1.

--------------------------------------------------------------------------
To configure a libunwind install for arm:

source build/envsetup.sh
lunch aosp_arm-eng
make -jXX
cd external/libuwnind
android/conf_arm.sh

To configure a libunwind install for mips:

source build/envsetup.sh
lunch aosp_mips-eng
make -jXX
cd external/libuwnind
android/conf_mips.sh

To configure a libunwind install for x86:

source build/envsetup.sh
lunch aosp_x86-eng
make -jXX
cd external/libuwnind
android/conf_x86.sh

--------------------------------------------------------------------------
The list of files generated by the configure scripts above:

include/config.h
include/libunwind.h
include/libunwind-common.h
include/tdep/libunwind_i.h
src/coredump/libunwind-coredump.pc
src/libunwind-generic.pc
src/ptrace/libunwind-ptrace.pc
src/setjmp/libunwind-setjmp.pc
src/unwind/libunwind.pc

--------------------------------------------------------------------------
The modifications made to base files:

src/arm/Gresume.c
  Add #include <asm/sigcontext.h> to top of file.

include/libunwind-mips.h
  Addr #include <asm/sigcontext.h>
  typedef struct {
    struct sigcontext uc_mcontext;
  } ucontext_t;

src/mips/getcontext.S
  #define LINUX_UC_MCONTEXT_GREGS -> LINUX_UC_MCONTEXT_SC_REGS 0
  #define LINUX_UC_MCONTEXT_PC -> LINUX_UC_MCONTEXT_SC_PC (8*32)
